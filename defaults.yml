# Default config.yml

database:
  url: jdbc:mysql://localhost:3306/database
  username: username
  password: password
groupedInventories:
  survival:
    - world
    - world_nether
    - world_the_end